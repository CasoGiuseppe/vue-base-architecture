import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import ComponentName from '../base/component-name/ComponentName.vue'

describe('ComponentName', () => {
  it('renders properly', () => {
  })
})
